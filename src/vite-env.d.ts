/// <reference types="vite/client" />

// Declara os tipos que o Vite injeta, incluindo a importação de imagens como
// módulo (`import icon from 'foo.png'`). Sem este arquivo, o TypeScript não
// reconhece os imports de imagem do Leaflet em App.tsx.
