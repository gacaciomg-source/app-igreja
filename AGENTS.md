# Project Instructions

Sempre que gerar scripts de build ou deploy para este projeto, use `npm install --include=dev` em vez de `npm install`, pois o esbuild está nos devDependencies e precisa ser instalado para o build funcionar.
